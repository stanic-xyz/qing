server:
  port: 8080
spring:
  application:
    name: qing-service-anime
  cloud:
    config:
      label: master
      profile: prod
      discovery:
        enabled: true
        service-id: qing-config-git
