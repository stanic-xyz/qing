apiVersion: v1
kind: Service
metadata:
  name: { { include "qing.fullname" . } }
  labels:
    { { - include "qing.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "qing.selectorLabels" . | nindent 4 } }
