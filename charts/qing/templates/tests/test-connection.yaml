apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qing.fullname" . }}-test-connection"
  labels:
    {{- include "qing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "qing.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
