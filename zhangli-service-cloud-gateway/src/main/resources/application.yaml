server:
  port: 9528
spring:
  application:
    name: spring-cloud-api-gateway
  cloud:
    gateway:
      routes:
        -id: url-proxy-1
          uri: https://blog.csdn.net
          predicates:
            -Path=/csdn