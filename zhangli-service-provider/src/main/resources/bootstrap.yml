spring:
  application:
    name: zhangli-service-provider
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        enabled: false
      discovery:
        server-addr: localhost:8848
        enabled: false
